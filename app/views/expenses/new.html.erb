<%= turbo_frame_tag "modal" do %>
  <%= render DialogComponent.new(open: true, close_on_backdrop: true) do |dialog| %>
    <% dialog.with_title { "Add expense" } %>
    <% dialog.with_body { render "form", expense: @expense } %>
  <% end %>
<% end %>
